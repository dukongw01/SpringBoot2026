<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.member.mapper.MemberMapper">

<!-- public int insertMember(MemberDTO mdto); -->
<!-- insert / delete / update문 resultType를 사용하지 않는다. -->
<!-- insert / delete / update문운 한 행의 결과를 int 반환한다. -->
<insert id="insertMember" parameterType="com.green.member.MemberDTO">
	INSERT INTO user_member(id,pw,mail,phone) VALUES(#{id},#{pw},#{mail},#{phone})
</insert>
<!-- select문은 resultType을 정의 한다. -->
<!-- public boolean isMember(String id); -->
<select id="isMember" parameterType="String" resultType="boolean">
	SELECT COUNT(*) FROM user_member WHERE id=#{id}
</select>

<!-- public List<MemberDTO> allSelectMember(); -->
<select id="allSelectMember" resultType="com.green.member.MemberDTO">
	SELECT * FROM user_member
</select>

<!-- public MemberDTO oneSelectMember(String id); -->

<select id="oneSelectMember" parameterType="String" resultType="com.green.member.MemberDTO"></select>

<!-- public int updateMember(MemberDTO mdto); -->	

<!-- public String getPass(String id); -->	

<!-- public int deleteMember(String id); -->	


</mapper>